# Issue Tracker

Issues live as markdown files under `.scratch/<feature>/` in this repo.

## Creating issues

Create a file at `.scratch/<feature>/<slug>.md`. Use the filename as the issue ID.

## Label format

Add a `labels:` frontmatter field listing active triage labels.

## Workflow

No CLI required. Read, write, and update files directly with standard file tools.
