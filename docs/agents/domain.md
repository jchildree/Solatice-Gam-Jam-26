# Domain Docs

Single-context layout.

## Locations

- `CONTEXT.md` -- project domain language and bounded contexts (repo root)
- `docs/adr/` -- architectural decision records (repo root)

## Consumer rules

Read `CONTEXT.md` before any domain-specific reasoning. Check `docs/adr/` for past decisions before proposing architecture changes. If `CONTEXT.md` does not exist yet, ask the user to create it or note the gap.
